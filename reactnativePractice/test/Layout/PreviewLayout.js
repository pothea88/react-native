import React from "react";
import {StylesSheet, View, Text} from 'react-native';

const PreviewLayout = (
    {
        label,
        children,
        values,
        selectedValue,
        setSelectedValue
    }
) => {
    return(
        <View></View>
    )
}
export default PreviewLayout;